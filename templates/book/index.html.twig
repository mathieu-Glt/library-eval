{% extends 'base.html.twig' %}

{% block title %}Book index{% endblock %}

{% block body %}
{# {{ dump(books) }} #}
    <h1>Book list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Year</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for book in books %}
            <tr>
                <td>{{ book.id }}</td>
                <td>{{ book.title }}</td>
                <td>{{ book.year }}</td>
                <td>
                    <a class="btn btn-primary" type="boutton" href="{{ path('app_book_show', {'id': book.id}) }}">show</a>
                    <a class="btn btn-primary" type="boutton" href="{{ path('app_book_edit', {'id': book.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" type="boutton" href="{{ path('app_book_new') }}">Create new</a>
{% endblock %}
