{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
{# {{ dump(book) }} #}

    <h1>Book</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ book.title }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ book.year }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" type="boutton" href="{{ path('app_book_index') }}">back to list</a>

    <a class="btn btn-primary" type="boutton" href="{{ path('app_book_edit', {'id': book.id}) }}">edit</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
